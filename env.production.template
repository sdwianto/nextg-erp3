# Production Environment Variables for Vercel Deployment
# Copy this content to Vercel Environment Variables

# Database Configuration (empty for mock data)
DATABASE_URL=
DIRECT_URL=

# Environment
NODE_ENV=production

# CouchDB for offline sync (empty for now)
NEXT_PUBLIC_COUCHDB_URL=

# Socket.io for real-time updates (empty for now)
NEXT_PUBLIC_SOCKET_URL=

# Skip environment validation
SKIP_ENV_VALIDATION=true
